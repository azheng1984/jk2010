<?php
namespace Hyperframework\Web;

class Action {
    protected function redirect($url, $statusCode = 301) {
        Application::redirect($url, $statusCodde);
    }
}

$this->redirect();
$ctx->get('article_id');
$ctx->get('article_id');
$this->getParam('article');
$ctx->get();

$ctx->get('action_result');
$ctx->get('app')->redirect();
$ctx->get('id');
