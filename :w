<?php
namespace Hyperframework;

class CacheLoader {
    public static function load($pathConfigName, $defaultPath) {
        return DataLoader::load(
            $pathConfigName, $defaultPath, 'cache', 'data/cache'
        );
    }
}
